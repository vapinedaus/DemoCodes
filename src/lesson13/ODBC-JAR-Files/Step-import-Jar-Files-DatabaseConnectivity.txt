Add a jars driver for SQL Server 2008 that should work by the following steps in Eclipse.


1. Right click project
2. select Properties -> select Build Path -> select Library Tab ->Click add external jars, and then navigate to the jtds-1.3.1 file).



Add a jars driver for Ms-Access that should work by the following steps in Eclipse.

1. Right click project
2. select Properties -> select Build Path -> select Library Tab ->Click add external jars, and then navigate to the following Jar files

1- hsqldb.jar

2- jackcess 2.0.4.jar

3- commons-lang-2.6.jar

4- commons-logging-1.1.1.jar

5- ucanaccess-2.0.8.jar

